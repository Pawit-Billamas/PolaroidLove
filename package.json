{
  "name": "photobooth-together",
  "version": "1.0.0",
  "private": true,
  "description": "Take a synced polaroid with someone on another device, live over a room code.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.22.2",
    "peer": "^1.0.2"
  }
}
